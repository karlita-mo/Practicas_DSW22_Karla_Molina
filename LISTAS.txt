fun main() {
    
   val numerosPares = mutableListOf<Int>()
    
    for (i in 1..100) {
        if (i % 2 == 0) {
            numerosPares.add(i)
        }
    }

    println("Números pares entre 1 y 100: $numerosPares")
    println("Tamaño de la lista: ${numerosPares.size}")
}
